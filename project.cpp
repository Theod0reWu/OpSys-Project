#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <string.h>

void fetch(char** args, int& n, int& seed, int& lambda, int& bound, int& cs, int& alpha, int& slice) {
	//use arguments as buffers
}

int main(int argc, char** argv) {
	//error handling
	
	//fetch args
	int n = 0, seed = 0, lambda = 0, bound = 0, cs = 0, alpha = 0, slice = 0;
	fetch(argv, n, seed, lambda, bound, cs, alpha, slice);
	
	//do FCFS
	
	//do SJF
	
	//do SRT
	
	//do RR
	
	//output
	printf("hello\n");
}